DEPLOY IMMEDIATO SENZA GIT
=========================

Accedi al server e modifica direttamente:

ssh root@78.47.123.128
cd /home/highlander/app

# Modifica il file problema
nano client/src/pages/forgot-username.tsx

# Cerca la funzione onSubmit e aggiungi prima di 'try':
console.log("DEBUG: Iniziando richiesta forgot-username");

# Dopo 'const response = await fetch':
console.log("DEBUG: Response ricevuta:", response.status, response.statusText);

# Dopo 'const result = await response.json()':
console.log("DEBUG: Result parsed:", result);

# Nel catch block:
console.error("DEBUG: Errore completo:", error);

# Salva (Ctrl+X, Y, Enter) e rebuild
npm run build
pm2 restart highlander

# Testa su https://highlandergame.it/forgot-username con console aperta