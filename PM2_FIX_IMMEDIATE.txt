CORREZIONE IMMEDIATA ECOSYSTEM.CONFIG.JS
======================================

Esegui questi comandi sul server:

# 1. Sostituisci ecosystem.config.js con sintassi corretta
cat > ecosystem.config.js << 'EOF'
module.exports = {
  apps: [{
    name: 'highlander',
    script: 'server/index.ts',
    interpreter: 'node',
    interpreter_args: '--loader tsx',
    cwd: '/home/highlander/app',
    env: {
      NODE_ENV: 'production',
      PORT: 5000,
      DATABASE_URL: 'postgresql://highlander:P3CQeyzh/YLiyxabFSMgwoxRpUPW5qw4@localhost:5432/highlander_db',
      SMTP_USER: 'support@highlandergame.it',
      SMTP_PASSWORD: 'Calibro9!',
      SESSION_SECRET: 'your-session-secret-here',
      BASE_URL: 'https://highlandergame.it'
    },
    instances: 1,
    autorestart: true,
    watch: false,
    max_memory_restart: '1G',
    error_file: './logs/error.log',
    out_file: './logs/out.log',
    log_file: './logs/combined.log',
    time: true
  }]
}
EOF

# 2. Crea directory logs
mkdir -p logs

# 3. Build applicazione
npm run build

# 4. Avvia PM2
pm2 start ecosystem.config.js

# 5. Verifica
pm2 list

# 6. Se funziona, salva configurazione
pm2 save

echo "PM2 configurato correttamente"
echo "Test app: https://highlandergame.it"