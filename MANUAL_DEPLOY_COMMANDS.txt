COMANDI PER DEPLOY MANUALE
=========================

STEP 1 - COMMIT E PUSH DA REPLIT:
```bash
git add .
git commit -m "Debug: Logging dettagliato per forgot-username in produzione

- Aggiunto logging completo frontend con emoji per identificare facilmente gli errori
- Aggiunto logging backend per tracciare richieste API
- Controllo Content-Type per verificare risposte JSON valide
- Gestione errori migliorata con stack trace completo"

git push origin main
```

STEP 2 - DEPLOY SUL SERVER:
```bash
ssh root@78.47.123.128
cd /home/highlander
./deploy.sh
```

STEP 3 - VERIFICA FUNZIONAMENTO:
1. Vai su https://highlandergame.it/forgot-username
2. Apri Developer Tools > Console
3. Inserisci email di test (es: dicostanzo.fabio@yahoo.it)
4. Clicca "Invia Username"
5. Controlla i log nella console

I log mostreranno:
🚀 = Richiesta inviata
📡 = Response ricevuta
📋 = Headers response
📄 = Content-Type
✅ = Dati parsati correttamente
💥 = Errore con stack trace

LOGS SERVER (se necessario):
```bash
ssh root@78.47.123.128
pm2 logs highlander --lines 50
```